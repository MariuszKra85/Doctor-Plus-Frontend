import {
  faTasks,
  faBarsProgress,
  faBell,
} from '@fortawesome/free-solid-svg-icons';
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';

const Task = () => <FontAwesomeIcon icon={faBarsProgress} />;

const Icon = () => <FontAwesomeIcon icon={faBarsProgress} />;
